# Example environment configuration for the ebook-tools Python services.
# Copy this file to `.env` and adjust values per environment. Any of the
# variables can be overridden by exporting them in the shell prior to startup.

# Comma- or space-delimited list of origins allowed to access the API via CORS.
EBOOK_API_CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Where the FastAPI service should look for the compiled SPA bundle. Leave unset
# to fall back to `web/dist` within the repository. Set to an empty value to
# disable static file hosting.
EBOOK_API_STATIC_ROOT=

# Optional Redis URL for persisting background job metadata. When unset the
# in-memory store is used.
JOB_STORE_URL=redis://localhost:6379/0

# Public base URL pointing at the backend storage routes. Used by helper
# services and the SPA to construct absolute download links.
EBOOK_STORAGE_BASE_URL=http://127.0.0.1:8000/storage

# Override the directories used by the pipeline runner when needed.
EBOOKS_DIR=books
EBOOK_WORKING_DIR=output
EBOOK_OUTPUT_DIR=output/ebook
EBOOK_TMP_DIR=tmp
