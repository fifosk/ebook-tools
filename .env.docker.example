# ebook-tools Docker Environment Configuration
#
# For local deployment:  cp .env.docker.example .env.docker
# For split-machine:     copy to each machine and adjust as needed.
#
# Values set here OVERRIDE those in docker-compose.yml environment section.
# Comment out lines to use the docker-compose.yml defaults instead.

# ── Backend Runtime ─────────────────────────────────────────────────

# CORS: space-separated origins allowed to access the API.
# Include both the production URL and local Docker frontend.
# EBOOK_API_CORS_ORIGINS=https://api.langtools.fifosk.synology.me https://langtools.fifosk.synology.me http://localhost:3080

# Disable built-in SPA serving (frontend is a separate Nginx container).
# EBOOK_API_STATIC_ROOT=

# TTS backend.  Options: gtts (internet required), piper (offline, needs voice models).
# macOS 'say' is NOT available in Linux containers.
# EBOOK_TTS_BACKEND=gtts

# Disable in-container RAMDisk; use tmpfs mount in docker-compose.yml instead.
# EBOOK_USE_RAMDISK=false

# Public URL for the storage endpoint (used by frontend to construct media URLs).
# EBOOK_STORAGE_BASE_URL=https://api.langtools.fifosk.synology.me/storage

# ── OAuth / Authentication ────────────────────────────────────────
# These are typically set in .env.local (volume-mounted into the container).
# Uncomment here to override or if not using .env.local.
# EBOOK_AUTH_GOOGLE_CLIENT_IDS=your-google-client-id.apps.googleusercontent.com
# EBOOK_AUTH_APPLE_CLIENT_IDS=com.example.app,com.example.app.tvos,com.example.app.web

# ── LLM / Translation ──────────────────────────────────────────────
# LLM_SOURCE=cloud
# OLLAMA_URL=http://host.docker.internal:11434/api/chat
# EBOOK_OLLAMA_API_KEY=

# ── Image Generation (Draw Things / Stable Diffusion) ──────────────
# Use host.docker.internal to reach services on the Mac Mini host.
# EBOOK_IMAGE_API_BASE_URL=http://host.docker.internal:7860
# EBOOK_IMAGE_CONCURRENCY=2

# ── Piper TTS (if using piper backend) ─────────────────────────────
# EBOOK_PIPER_MODELS_PATH=/models/piper-voices

# ── Optional ────────────────────────────────────────────────────────
# EBOOK_JOB_MAX_WORKERS=4
# EBOOK_THREAD_COUNT=8
# EBOOK_QUEUE_SIZE=20
