# Example environment configuration for the ebook-tools single-page application.
# Duplicate this file as `.env` or `.env.local` and tweak the values for your
# deployment target.

# Base URL for API requests made by the frontend.
VITE_API_BASE_URL=http://127.0.0.1:8000

# Endpoint for downloading or streaming stored assets from the backend. The UI
# can use this to build absolute URLs to generated files. Mirror the
# `EBOOK_STORAGE_BASE_URL` value configured for the API.
VITE_STORAGE_BASE_URL=http://127.0.0.1:8000/storage

# OAuth client IDs for web sign-in.
VITE_GOOGLE_CLIENT_ID=
VITE_APPLE_CLIENT_ID=

# Optional override for the Apple Sign In redirect URI.
VITE_APPLE_REDIRECT_URI=
