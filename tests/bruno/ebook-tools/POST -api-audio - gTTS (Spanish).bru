meta {
  name: POST /api/audio - gTTS (Spanish)
  type: http
  seq: 6
}

post {
  url: {{base_url}}/api/audio
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: audio/mpeg
}

body:json {
  {
    "text": "Hola mundo, bienvenidos a ebook-tools!",
    "language": "es",
    "voice": "gTTS"
  }
}

tests {
  test("Status 200", () => {
    expect(response.status).toBe(200);
  });

  test("MIME type is audio/mpeg", () => {
    expect(response.headers["content-type"]).toContain("audio/mpeg");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
