meta {
  name: POST /api/audio - macOS auto male (Japanese)
  type: http
  seq: 7
}

post {
  url: {{base_url}}/api/audio
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
  Accept: audio/mpeg
}

body:json {
  {
    "text": "こんにちは、ebook-toolsへようこそ！",
    "language": "ja",
    "voice": "macOS-auto-male"
  }
}

tests {
  test("Status 200", () => {
    expect(response.status).toBe(200);
  });

  test("MIME type is audio/mpeg", () => {
    expect(response.headers["content-type"]).toContain("audio/mpeg");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
